
<div class="container d-flex justify-content-center align-items-center vh-100">
  <div class="card shadow-lg p-4 border-0 rounded-4 text-center" style="max-width: 400px; width: 100%;">
    <div class="mb-3">
      <i class="bi bi-person-circle text-primary" style="font-size: 4rem;"></i>
    </div>
    
    <h2 class="fw-bold text-dark">Welcome, <%= @user.name %>!</h2>
    
    <div class="mt-3">
      <p class="text-muted"><i class="bi bi-envelope-fill text-primary"></i> <strong>Email:</strong> <%= @user.email %></p>
      <p class="text-muted"><i class="bi bi-person-badge-fill text-success"></i> <strong>Role:</strong> <span class="badge bg-secondary"><%= @user.role.capitalize %></span></p>
    </div>

  
  </div>
</div>
