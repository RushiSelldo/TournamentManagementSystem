<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Sign Up</title>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css">
</head>
<body class="bg-light">

  <div class="container d-flex justify-content-center align-items-center vh-100 " style="margin-top: 50px;">
    <div class="card shadow-lg p-4 border-0 rounded-4" style="max-width: 450px; width: 100%;">
      <div class="text-center mb-3">
        <i class="bi bi-person-plus-fill text-primary" style="font-size: 3.5rem;"></i>
        <h2 class="fw-bold text-dark mt-2">Sign Up</h2>
        <p class="text-muted">Create your account</p>
      </div>

      <%= form_with url: signup_path, method: :post, local: true, scope: :user, class: "needs-validation" do |form| %>
        <div class="mb-3">
          <%= form.label :name, class: "form-label fw-semibold" %>
          <%= form.text_field :name, class: "form-control rounded-3", placeholder: "Enter your full name", required: true %>
        </div>

        <div class="mb-3">
          <%= form.label :email, class: "form-label fw-semibold" %>
          <%= form.email_field :email, class: "form-control rounded-3", placeholder: "Enter your email", required: true %>
        </div>

        <div class="mb-3">
          <%= form.label :password, class: "form-label fw-semibold" %>
          <%= form.password_field :password, class: "form-control rounded-3", placeholder: "Enter a secure password", required: true %>
        </div>

        <div class="mb-3">
          <%= form.label :password_confirmation, class: "form-label fw-semibold" %>
          <%= form.password_field :password_confirmation, class: "form-control rounded-3", placeholder: "Confirm your password", required: true %>
        </div>

    
        <div class="d-grid">
          <%= form.submit "Sign Up", class: "btn btn-primary fw-semibold rounded-3 py-2" %>
        </div>
      <% end %>

      <div class="text-center mt-3">
        <p class="text-muted">Already have an account?</p>
        <%= link_to "Login Here", login_path, class: "btn btn-outline-secondary rounded-3 fw-semibold px-4" %>
      </div>
    </div>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
